import styled from 'styled-components';

const AudioRecordForm = styled.form`
    // background-color: ${({ theme }) => theme.color.background};
    display: flex;
    flex-direction: column;
    width: 400px;
    padding-top: 16px;
    gap: 16px;
`;

export { AudioRecordForm };
